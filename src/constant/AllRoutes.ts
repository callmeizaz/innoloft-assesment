import { Routes as routes } from '.';

export const PROTECTED_ROUTES = [
  routes.protected.home,
  routes.protected.product,
  routes.protected.accessDenied,
  routes.protected.pageNotFound,
];

export const PUBLIC_ROUTES = [];
